# ロードマップ

English: [ROADMAP.md](ROADMAP.md)

suco Pay は開発初期です。動かして確かめなくて済むように、今できることとできないことを書きます。

## 今できること

- 設定ファイルを読み、解決した値とそれぞれの出所を報告します。
- API の資格情報を発行し、一覧し、失効させます。
- account がどの資産を受け取るか、資産ごとの受取アドレスを記録します。
- API で支払いを作り、読み戻します。
- 支払いに対して、支払者が署名する署名データ（EIP-3009 の `TransferWithAuthorization`）を
  発行します。
- EVM のチェーンを `round`（チェーンを読む 1 回）ごとに読み、発行した `nonce` を使った送金を
  突き合わせ、規則が何と判定したかとともに記録します。
- 確定した支払いの返金を作り、加盟店が額を送り返すために署名する署名データ（EIP-712 の
  typed data）を発行します。
- 何に届いているか、見ている `network` がそれぞれどの状態かを答えます。

## まだできないこと

支払いは `succeeded` に届きます。確定判定は `network` ごとに設定できます。期限を過ぎた
支払いは待ったうえで `expired` になります。確定の判定と期限切れの流れは、
プロセスの中のチェーンに対しては端から端まで、Polygon に対しては実際にあった支払い 1 件で
1 段ずつ通しました。

- [ ] 支払い: ライフサイクル、期限、過払いと不足、冪等性
- [x] 確定: `network` ごとに設定する確定判定
- [ ] suco Checkout: インスタンス（`suco serve` のプロセス 1 つ）が配信する支払いページ。加盟店は支払者を支払いページへ送ります
- [ ] suco Component: 同じ支払いを、加盟店のページの中に埋め込む部品
- [ ] suco Link: 特定の商品を買う固定の URL
- [ ] フロントエンド SDK: 加盟店のページが suco Component を埋め込むのに使う SDK
- [x] Webhook: 署名、再送、通知の記録
- [x] 返金: 記録と送金指示の作成
- [ ] 照合: 内部状態とチェーンの定期突合
- [ ] Console: 支払いと送金の照会、返金、通知
- [ ] バックエンド SDK: PHP と Java と Go、API の記述からの生成
- [x] CLI: `init` `serve` `doctor` `credential` `asset` `payment` `network`

## 決まっていること、決まっていないこと

上の並びは作る順で、日程ではありません。上の一覧に、時期の約束はありません。
インストール用のスクリプトとビルド済みのバイナリは最初のリリースに付きます。

リポジトリを watch するか、必要なことを
[Discussions](https://github.com/sucopay/sucopay/discussions) に書いてください。
