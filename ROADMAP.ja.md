# ロードマップ

English: [ROADMAP.md](ROADMAP.md)

suco Pay は開発初期です。動かして確かめなくて済むように、今できることとできないことを書きます。

## 今できること

- 設定文書を読み、解決した値とそれぞれの出所を報告します。
- API の資格情報を発行し、一覧し、失効させます。
- どの資産を受け取るか、それぞれをどのアドレスへ払うかを記録します。
- API で payment を作り、読み戻します。
- payment に対して、支払者が署名する鍵を EIP-3009 の認可として発行します。
- EVM のチェーンを周ごとに読み、発行した鍵を使った送金を突き合わせ、規則が何と判定したかと
  ともに記録します。
- 何に届いているか、見ている network がそれぞれ何になったかを答えます。

## まだできないこと

payment を `succeeded` にするものがありません。送金は見つかり、突き合わされ、記録されますが、
それが決済として確定したと判断するのは下の確定判定で、まだありません。それまで payment は
`awaiting_payment` のままです。

- [ ] Payment: ライフサイクル、期限、過払いと不足、冪等性
- [ ] Finality: ネットワークごとに設定する確定判定
- [ ] 支払い画面: ホスト型と埋め込み型
- [ ] Webhook: 署名、リトライ、送信履歴
- [ ] Refund: 記録と送金指示の作成
- [ ] 照合: 内部状態とチェーンの定期突合
- [ ] Console: Payment と Transaction の照会、返金、Webhook 送信履歴
- [x] CLI: `init` `serve` `doctor` `credential` `asset` `payment` `network`

## 決まっていること、決まっていないこと

上の並びは作る順で、日程ではありません。時期の約束はここにありません。インストール用の
スクリプトとビルド済みのバイナリは最初のリリースに付きます。

リポジトリを watch するか、必要なものを
[Discussions](https://github.com/sucopay/sucopay/discussions) に書いてください。
