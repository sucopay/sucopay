# ライティングガイド

suco Pay の文書は、読者が次の行動を迷わず選び、安全に完了できるように書きます。文体を
揃えることより、読者の目的、読む順番、正確さを優先します。

## 基本方針

### 読者の目的から組み立てる

書き始める前に、読者と、読み終えたときに達成できることを 1 つ決めます。実装の内部構造や
書き手が説明しやすい順番ではなく、読者が作業する順番で情報を並べます。

導入ガイドでは、最短で成功する手順を先に示します。選択肢、例外、詳しい仕様は、基本の手順を
終えた後か、判断が必要になる箇所に置きます。未実装の機能や重大な制約は、読者がその機能を
試す前に明記します。

### 文書の役割を分ける

ページごとに中心となる役割を 1 つ決めます。作業に必要な補足説明や参照表まで別ページへ追い出す
必要はありませんが、チュートリアルの途中に網羅的な仕様を挟むなど、主目的を妨げる混在は避けます。

| 種類 | 読者が求めるもの | 基本の構成 |
|---|---|---|
| チュートリアル | 初めて動かすための案内 | 到達点、前提、手順、成功の確認、次の作業 |
| ハウツー | 特定の作業を終える手順 | 目的、前提、手順、失敗したときの対処 |
| リファレンス | 正確な仕様 | 項目、型、既定値、制約、レスポンス、エラー |
| 解説 | 仕組みや判断材料 | 結論、背景、影響、関連する仕様 |

表の構成は出発点であり、必須項目のチェックリストではありません。読者の判断や作業に不要な節は
作らず、必要な情報がこの型に収まらない場合は目的に合わせて構成を変えます。

リファレンスを最初から順に読ませないでください。初めて使う読者にはガイドから案内し、必要な
項目へリンクします。用語集は必須ではありません。ページ固有の語は、最初に必要になる箇所で
短く定義します。

### 次の行動を示す

ページの冒頭で、対象読者と得られる結果を伝えます。末尾では、作業の自然な続きだけを案内します。
関連するという理由だけでリンクを並べないでください。

## ドキュメントの導線

[docs/README.md](docs/README.md) と [docs/README.ja.md](docs/README.ja.md) が開発者向け
ドキュメントの入口です。初めて使う読者には、次の順番を案内します。

1. README でローカル起動
2. `configuration` でネットワークと資産を設定
3. `api` と `checkout` で支払いを実装
4. `webhooks` で確定結果を受信
5. `refunds` で返金を実装
6. `operating` で本番運用を準備

文書ごとの主な読者と役割は次のとおりです。

| 文書 | 主な読者 | 役割 |
|---|---|---|
| README | 初めて評価する人 | 製品の概要とローカル起動 |
| docs/README | 開発者、運用者 | 目的別の入口と推奨順序 |
| docs/api | 加盟店の開発者 | 支払い API のリファレンス |
| docs/checkout | 加盟店の開発者 | 支払いページの組み込み |
| docs/webhooks | 加盟店の開発者 | Webhook 受信の実装と運用 |
| docs/refunds | 加盟店の開発者 | 返金フローの実装 |
| docs/configuration | 運用者 | `suco.yaml` の設定リファレンス |
| docs/operating | 運用者 | ヘルスチェックと障害対応 |
| CONTRIBUTING | 貢献者 | 開発手順と規約 |
| ROADMAP | 評価する人 | 実装済みの機能と制限 |
| SECURITY | 脆弱性を見つけた人 | 非公開の報告手順 |

## 文体

- 日本語はです・ます調、英語は現在形を基本にします。
- 結論または操作を先に書き、理由は必要な箇所に続けます。
- 1 段落では 1 つの話題を扱います。長い段落は、見出し、表、手順に分けます。
- 手順は動詞から始めます。誰が操作するか曖昧になる場合だけ主語を補います。
- API 名、設定名、状態名など、コードに現れる値はコード書体にします。
- 同じ概念には同じ語を使います。言い換えによる変化を付けません。
- 読者に直接関係しない設計史、一般論、締めの格言は削ります。
- 「簡単に」「もちろん」「ただ〜するだけ」のように、難しさを決めつける表現は使いません。
- 不要な擬人化、会話調、感嘆符、宣伝調の形容詞は避けます。

指示語や代名詞は、参照先が明らかなら使えます。同じ名詞を機械的に繰り返して不自然な文に
しないでください。一方で、段落をまたぐ曖昧な「これ」「それ」は具体的な名前に置き換えます。

禁止語の一覧で文章を作らないでください。自然さと正確さが両立する語を、文脈ごとに選びます。
たとえば、ソフトウェアの動作には「返す」「出力する」「拒否する」を使い分けます。

## 見出しとページ構成

見出しだけを読んでも、ページの流れが分かるようにします。見出しは読者の作業や探している情報を
表し、「概要」「詳細」のような範囲の曖昧な見出しを避けます。

ページの型は固定しません。次の要素は必要な場合だけ使います。

- 言語切り替えのリンク
- 対象読者と前提
- 機能の提供状況や重大な制約
- 完了を確認する方法
- 次に行う作業へのリンク

表は、同じ属性を持つ項目を比較するときに使います。操作手順は番号付きリスト、選択肢は箇条書き、
前後関係や状態遷移は Mermaid の図を使います。短い説明まで表に押し込まないでください。

## API リファレンス

API エンドポイントは、次の順を基本にします。

1. 何をするエンドポイントか
2. メソッドとパス
3. 必要な権限
4. リクエスト例
5. パラメータ
6. 成功レスポンス例
7. レスポンス項目
8. このエンドポイント固有のエラー

共通する認証、冪等性、エラー形式は 1 か所で説明し、各エンドポイントから参照します。
リクエスト例には実在する秘密を使いません。トークンは環境変数または `<token>` のような
プレースホルダーで示します。本番の URL には HTTPS を使います。

## 注意書き

注意書きは、読み飛ばすとデータ損失、二重処理、秘密の漏えい、誤送金につながる場合に使います。
通常の前提や補足を注意書きにしないでください。

ラベルは次の 2 種類です。

- `> **重要:**` 読者が操作する前に知る必要がある制約
- `> **注:**` 本筋を止めずに示す補足

## 用語

製品名は suco Pay、リポジトリ名は `sucopay`、コマンド名は `suco` です。主要な用語は次の
表記に揃えます。

| 概念 | 日本語 | 英語 | コード上の表記例 |
|---|---|---|---|
| API が管理する支払い | 支払い | payment | `payment`, `/payments` |
| 返金 | 返金 | refund | `refund`, `/refunds` |
| 支払者が署名する 1 回分 | 支払い試行 | attempt | `attempt` |
| API の認証情報 | 資格情報 | credential | `Authorization` |
| チェーン上のトークン | 資産 | asset | `assets` |
| チェーン上の送金 | 送金 | transfer | `transfer` |
| 支払いの受取先 | 受取アドレス | destination address | `destination` |
| Webhook の送信先 | Webhook エンドポイント | webhook endpoint | `webhook_endpoints` |
| Webhook で送る 1 件 | 通知 | delivery | `deliveries` |
| Webhook の検証用秘密 | 署名シークレット | signing secret | `secret`, `whsec_…` |
| チェーンの接続先 | RPC エンドポイント | RPC endpoint | `rpc.own`, `rpc.others` |
| `suco serve` の 1 プロセス | インスタンス | instance | |
| 支払者が開くページ | 支払いページ | payment page | `checkout_url` |
| 返金に署名するページ | 署名ページ | signing page | `refund_url` |

初出の略語は、対象読者に一般的でない場合だけ展開します。規格名や API の識別子は翻訳しません。

## 英語版と日本語版

英語版を仕様上の正とし、日本語版には同じ事実と作業の流れを記載します。逐語訳は避け、各言語で
自然な語順と見出しにします。コード例、URL、識別子は両言語で揃えます。

片方を変更したら、同じコミットでもう片方を変更します。見出しの数を機械的に揃えることより、
読者が同じ手順、制約、注意事項へ到達できることを確認します。

## 事実の正典

同じ仕様を複数のページに詳しく書かないでください。別のページでは、作業に必要な要点だけを示して
正典へリンクします。

| 仕様 | 正典 |
|---|---|
| 支払いの状態、金額の単位、認証、冪等性、共通エラー | docs/api |
| 支払いページ、支払い試行、署名データ | docs/checkout |
| Webhook のイベント、署名、再送 | docs/webhooks |
| 返金の状態、作成、署名 | docs/refunds |
| 設定項目と既定値 | docs/configuration |
| ヘルスチェック、診断、復旧コマンド | docs/operating |

## 表記

- 半角の英数字と全角文字の間には半角スペースを入れます。
- 数字は半角にし、単位の前に半角スペースを入れます（`5 秒`、`64 KiB`）。
- カタカナ語は長音を省きません（サーバー、ユーザー、プロバイダー）。
- 日本語の句読点は「、」「。」を使います。
- 行は読みやすい位置で約 100 桁に折り返します。表とコードブロックは折り返しません。
- 図は Mermaid で書き、装飾目的では使いません。

## 編集後の確認

1. 新しい読者が、入口から目的のページへ進めるか確認します。
2. 基本の成功手順が、例外や詳細に遮られていないか確認します。
3. 未実装の機能と重大な制約が、試す前に見えるか確認します。
4. コマンド、設定、レスポンス例を実装と照合します。
5. 英語版と日本語版の手順、例、制約、注意書きを照合します。
6. リンクと `make check` を確認します。
