# ライティングガイド

このリポジトリの文書を書くときの決まりです。README、docs、CONTRIBUTING、コードのコメントに
適用します。日本語の文書と英語の文書の両方に適用し、片方だけの節はそう書いてあります。

## 読者と文書の種類

文書ごとに読者は 1 種類です。読者が要る情報を、要る順に置きます。

| 文書 | 読者 | 種類 |
|---|---|---|
| README | 初めて触る人 | 入口。動く手順と、docs への案内 |
| docs/api | 加盟店の開発者 | リファレンス。先頭に支払いの流れと状態遷移 |
| docs/webhooks | 加盟店の開発者 | 受信側の要件と、リファレンス |
| docs/checkout | 加盟店の開発者 | 支払いページの仕組みと、組み込みに要ること |
| docs/refunds | 加盟店の開発者 | 返金の仕組みと、リファレンス |
| docs/configuration | 運用者 | リファレンス。設定の表と、設定を選ぶ理由 |
| docs/operating | 運用者 | 手順書。症状から対処へ |
| CONTRIBUTING | 貢献者 | 決まりと手順 |
| ROADMAP、SECURITY | 誰でも | 今できることと、報告の窓口 |

英語版の you は、その文書の読者です。加盟店向けの 4 本では加盟店の開発者、運用者向けの
2 本では運用者です。その文書の中では、その人を三人称で呼びません。支払者は常に payer です。
日本語版は主語を省きます。第三者として言うときは「加盟店」「運用者」「支払者」と書き、
「あなた」は書きません。

## ページの型

すべてのページは次の順です。

1. H1
2. もう片方の言語へのリンク
3. リード。そのページで読者ができるようになることを 1 文で書きます
4. 読者と前提。1 文から 2 文です
5. `## このページの語`。そのページで初めて出す語を、読む順に並べた表です。語と意味の 2 列
6. 本文
7. `## 関連`。次に読むページへのリンクです

リファレンスの項目（API のエンドポイントと設定）は、目的の 1 文、リクエストの例、
パラメータの表（名前、型、必須、説明）、レスポンスの例、レスポンスの項目の表、失敗の表
（ステータス、いつ、対処）の順です。設定は設定、既定値、説明の 3 列です。API エンドポイントの
表の第 1 列は「パス」です。

手順書の節は、症状を見出しにし、意味と対処の表を置きます。

一覧できるものは、散文にせず表にします。項目の一覧、状態の一覧、失敗の一覧がそうです。

注記は「注:」と「注意:」の 2 種類だけです。読者の行動を変えるものにだけ使い、前提や手順や
必須の情報を注記にしません。

## 事実の正典

同じ事実は 1 つの文書に置き、ほかの文書はそこへリンクします。正典は次のとおりです。

| 事実 | 正典 |
|---|---|
| 支払いの状態と遷移、`transfer` の項目、単位の規則、URL をログに残さないこと、`Idempotency-Key` | docs/api |
| 返金の状態と遷移 | docs/refunds |
| Webhook エンドポイントの経路と通知 | docs/webhooks |
| ページの経路のセキュリティヘッダと CSP | docs/api の経路の表の注 |
| module が未公開である注記、iframe を断ること、typed data の説明 | docs/checkout |
| `/readyz` の語の定義 | docs/operating |
| 設定の項目と既定値、`others` の一致の仕組み、期限切れの判定 | docs/configuration |

## 文

物事はその名前で呼びます。「〜するもの」「〜する先」「そこ」で物事を指しません。名前が無い
物事は、初出で名前を決めて定義します。

```
悪い: 経路は、Payment を作るものと読むもの、それをサーバに知らせる先を扱うものです。
良い: API エンドポイントは 3 つに分かれます。支払いを作る `POST /payments`、支払いを読む
      `GET /payments/{id}`、Webhook エンドポイントを管理する `/webhook_endpoints` です。
```

指示語は、同じ文の中の名詞を受けるときだけ使います。前の文の物事を「それ」「その」「これ」
「この」「そこ」で指しません。名詞を繰り返します。文頭の指示語は検査で落とします。リードの
「このページは」だけが例外です。

```
悪い: 鍵は、それが作った支払いと同じだけ残ります。
良い: キーは、キーで作った支払いと同じ期間残ります。
```

新しい語は、初出で定義してから使います。定義の前に使いません。1 文に新しい語は 1 つです。
定義は `## このページの語` の表に置き、読む順に並べます。

1 文に 1 つのことを書きます。「〜ので、」「〜が、」で 2 つのことを繋がないでください。
日本語は 1 文 100 字を超えないようにします。英語は 40 語を超えないようにします。

```
悪い: 受け取り側の持ち時間は接続を含めて 20 秒で、それより遅い応答は失敗の試行として数え、
      再送します。
良い: 受け取り側の持ち時間は、接続を含めて 20 秒です。それより遅いレスポンスは失敗した試行
      として数え、再送します。
```

理由を書くのは、それを読まないと読者が誤った設定や実装をするときだけです。その行動の直後に
1 文で書きます。設計の経緯と代案は書きません。

残す理由の例です。

- `awaiting_finality` が状態である理由。読まないと、加盟店が期限を終点とみなして注文を閉じる
- `misses` を 1 にできない理由。読まないと、運用者が最小値を選ぼうとする
- `sslmode` を弱めると通信の途中で何が読めるか。読まないと、運用者が TLS を緩める
- `/healthz` が何も参照しない理由。読まないと、liveness probe をデータベースに向ける
- `--force` で失うもの。読まないと、同じ額が二度出る
- checksum を見る理由。読まないと、reference の打ち間違いを軽く見る

```
悪い: 図は Mermaid で書きます。GitHub が描画し、あとから編集できます。
良い: 図は Mermaid で書きます。
```

節の最後の文が、前の文に事実を足していなければ削ります。格言の形か事実の形かは問いません。

```
悪い: …checksum が一致しない reference は断ります。チェーンは資金を返しません。
悪い: 復旧は設計要件であって、運用の後付けではありません。
```

規則を述べている文は残します。「欄に無い asset は『止まっていない』ではありません」は、
欄に無いものを安全側に読むなという規則です。

「A ではなく B」は、読者が持ちそうな誤解を正すときだけ使います。それ以外は B だけ書きます。

```
良い: `behind` は latest ではなく確定ブロックからの差です。（latest を期待する読者を正す）
悪い: Reconciliation は監視ではなく修復のための仕組みです。（誤解の想定が無い）
```

「することができる」「を行う」「が可能です」を使いません。二重否定を使いません。
「かもしれない」「と思います」「現在は」のような弱い語を使いません。

受動態は、機械が自動でする動作にだけ使います。人の操作は能動態で書きます。

```
良い: ページが表示されます。
良い: 加盟店のサーバーが `POST /payments` を呼びます。
悪い: `POST /payments` が呼ばれます。
```

モノに人間の動詞をさせません。誰が何をするかを書きます。

```
悪い: Console が最初の Payment まで案内します。
良い: Console は http://localhost:7826 で起動します。
```

結論と条件を先に書きます。「条件 → 指示」「場所 → 動作」の順です。段落は 1 つの話題で、
先頭の文がその話題を言います。

依頼は「〜してください」と書きます。禁止は「〜しないでください」と書き、その後に代わりに
することを書きます。

## 表記

- 本文はです・ます調です。箇条書きと表は、1 つのまとまりの中で文体を揃えます。見出しは
  名詞句にします
- 半角の英数字と全角の文字の間に半角スペースを入れます。全角の文字どうしの間には
  入れません
- カタカナの語尾の長音は付けます。サーバー、ユーザー、プロバイダー、インターフェース、
  アダプター
- 数字は半角で、単位の前に半角スペースを入れます。5 秒、24 時間、2048 バイト。英語版も
  算用数字で書きます
- 句読点は「、」と「。」です。中黒で 3 つ以上を並べません。挿入のダッシュ（—）を
  使いません。読点、コロン、カッコのどれかにします
- 形式名詞、補助動詞、接続詞はひらきます。「〜すること」「〜してください」「および」
  「または」「ただし」
- 略語は初出で展開します。「MPC（複数当事者計算）」「再編成（reorg）」
- 見出しに数を入れません。項目が増えると見出しとアンカーが変わります
- 行は 98 桁（全角は 2 桁）で折り返します。表とコードブロックは折り返しません。日本語の
  折り返しは、読点か句点の後か、語と語の間で行います。語の途中で折り返しません。表示では
  改行が半角スペースになるからです

## 用語

コードに現れる語は、その字面を指すときだけコードの書体で書きます。それ以外は概念の語で
書きます。初出で「支払い（`payment`）」のように併記します。製品名と規格名はそのままです。
英語の語を漢語に写しただけの訳（delivery を配送）は使いません。日本語でその物事を指す語で
書きます（delivery は通知）。
suco Pay、Polygon、JPYC、EIP-712、EIP-3009、Standard Webhooks がそうです。製品名は
suco Pay、リポジトリとパッケージとコマンドは `sucopay` と `suco` です。

| 概念 | 日本語 | 英語 | コードで指すとき |
|---|---|---|---|
| API の支払いの object | 支払い | payment | `payment`、`/payments` |
| 払うという行為 | 払う | pay | |
| 支払者が払おうとする 1 回 | 支払い試行 | attempt | `attempt` |
| 返金 | 返金 | refund | `refund`、`/refunds` |
| account | account | account | `account` |
| credential | 資格情報 | credential | `suco credential` |
| asset | 資産 | asset | `assets` |
| 資産の識別子 | `reference` | reference | `reference` |
| network（`suco.yaml` の） | `network` | network | `networks` |
| IP のネットワーク | ネットワーク | network | |
| チェーン | チェーン | chain | |
| コントラクト | コントラクト | contract | |
| RPC の provider | RPC プロバイダー | provider | `rpc.own`、`rpc.others` |
| node | ノード | node | |
| チェーン上の送金 | 送金 | transfer | `transfer` |
| reorg | 再編成（reorg） | reorganisation | |
| 確定 | 確定 | settle、final | `succeeded` |
| Webhook の受け口 | Webhook エンドポイント | webhook endpoint | `webhook_endpoints` |
| API の経路 | API エンドポイント | route | 表の列は「パス」 |
| RPC の接続先 | RPC エンドポイント | RPC endpoint | `rpc.own` |
| 1 つの event の 1 回の送信 | 通知 | delivery | `deliveries` |
| 通知の 1 回の試み | 通知の試行 | delivery attempt | `attempts` |
| Webhook の秘密 | 署名シークレット | signing secret | `secret`、`whsec_` |
| 暗号の鍵 | 鍵 | key | `credentials.key` |
| 設定や JSON のキー | キー | key | |
| `Idempotency-Key` の値 | キー | key | `Idempotency-Key` |
| EIP-3009 の nonce | `nonce` | nonce | `nonce` |
| 結果を読める URL | 「この URL を持つ人は結果を読めます」と書く | | `checkout_url`、`refund_url` |
| HTTP の要求と応答 | リクエスト、レスポンス | request、response | |
| URL の path | パス | path | |
| 通信路 | 通信の途中 | on the wire | |
| 加盟店 | 加盟店 | you | |
| 支払者 | 支払者 | payer | |
| 運用者 | 運用者 | you | |
| `suco serve` のプロセス 1 つ | インスタンス | instance | |
| 全体 | 語を立てない。インスタンスで足り、全体なら「suco Pay 全体」 | deployment | |
| `suco.yaml` | 設定ファイル | configuration file | `suco.yaml` |
| チェーンを読む 1 回 | `round`（初出で「チェーンを読む 1 回」） | round | `no-round` |
| 読んだ位置 | `cursor` | cursor | `network cursor` |
| 支払いを受け取るアドレス | 受取アドレス | destination address | payment の `destination` |
| 返金が戻る先 | 返金先アドレス | refund address | refund の `destination` |
| 支払者のページ | 支払いページ | payment page | `checkout_url` |
| 加盟店が署名するページ | 署名ページ | signing page | `refund_url` |
| ページを返すこと | 配信する | serve | |
| stdout に書く | 出力する | print | |
| 外の相手に尋ねる | 問い合わせる | ask | |
| 金 | 場面で分ける。額、残高、着金 | amount、balance、funds | |
| 加盟店のサーバー | 加盟店のサーバー | your server | |

使わない語です。`scripts/check-docs.sh` が docs と README の日本語版で検査します。CONTRIBUTING、
ROADMAP、SECURITY も同じ規則で書き、検査は後から広げます。

- 印字（「印字可能な ASCII」を除く）、訊く、配備、宛先、経路、要求、応答、契約（コントラクトの
  意味で）、周、配送
- 文頭の「それ」「その」「これ」「この」「そこ」。動詞に付いた「もの」（「署名するもの」）
- 長音なしのサーバ、ユーザ、インタフェース、アダプタ、エクスプローラ、プロバイダ
- 地の文の Payment と Refund。Webhook は日本語版では大文字のまま使い、英語版は webhook です
- 裸の「エンドポイント」。API エンドポイント、RPC エンドポイント、Webhook エンドポイントの
  どれかを書きます

## 英語版

- 現在形で書きます
- 行為者が you のときは能動態です。suco とチェーンが行為者の受動態はそのままでよいです
  （"a transfer can be reorganised away"）
- セミコロンで文を繋ぎません。挿入のダッシュ、"say"、慣用句、ユーモアを使いません
- 1 つの概念に 1 つの語です。代名詞は先行詞が 1 つに決まるときだけ使います
- 見出しは名詞句です。"Registering an endpoint" ではなく "Endpoint registration" です

## 英日の運用

- 英語版が正で、日本語版は同じ事実を、同じ見出しの下に、日本語の順で書きます。逐語訳を
  しません。比喩の直訳（「経路にいる者」）は、対象そのものを指す語にします（「通信の途中で
  これを見られる人」）
- 見出しは英日で 1 対 1 です。数も順も揃えます。コードの語だけの見出しは、英日で同じ大小に
  します
- コード例、URL、識別子は訳しません
- 両方を同じ commit で直します

## 図

図は Mermaid で書きます。ASCII アートは使いません。

````
```mermaid
flowchart LR
  customer["顧客ウォレット"] -->|JPYC| merchant["加盟店ウォレット"]
```
````

`text` フェンスはリスト（環境変数、イベント名、フィールド一覧）とディレクトリツリーに限ります。

## 未完成のもの

書く予定の項目を並べた見出し（`## Outline`）や `TBD` を残さないでください。

決まっている事実だけで短く書くか、そのファイルを作らないかのどちらかにします。

## 公開しないもの

このリポジトリは公開されます。次の内容は書きません。

- 事業上のターゲット区分
- 未公表の数値目標
- 内部資料の所在

## 編集後の確認

文書を書き換えたら、次を確認します。

- 削った内容を指していた指示語が残っていないか。`他の` `その` `これ` `also` `other` `that` で
  検索し、指示対象が同じ文書内にあるかを 1 つずつ確認します
- 本文の中の数が本文と合っているか。「2 つの副命令」と書いて 4 つ説明していないか
- 書き換えた段落を折り返し直したか。段落の途中で極端に短い行が残っていないか
- `make check` が通るか
